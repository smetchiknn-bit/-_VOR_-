import { useState } from "react";
import type { VorResult } from "../lib/vor";
import { CountUp, IconAlert, IconDownload, IconCheck, Reveal, TaChip } from "./ui";

export function Results({ res, xlsxUrl, csvUrl }: { res: VorResult; xlsxUrl: string | null; csvUrl: string | null }) {
  return <section>Results</section>;
}
